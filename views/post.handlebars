<div>
        <div class="card">
      <div class="card-body">
        <h1>{{title}}</h1>
      </div>
    </div>
    
    <div class="card">
      <div class="card-body">
        <h1>{{body}}</h1>
      </div>
    </div>
    
    <div class="card">
      <div class="card-body">
        <h1>{{User.username}}</h1>
      </div>
    </div>
</div>