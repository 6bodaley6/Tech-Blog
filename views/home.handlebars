<h1>hello</h1>
{{#each posts}}
<h1>{{title}}</h1>
<h3>{{body}}</h3>
<h4>{{User.username}}</h4>

{{/each}}
{{!-- handlebars for(loop) above #each--}}
{{!-- look up boot strap and copy a card set up and put in classses try to make it look nice  --}}
