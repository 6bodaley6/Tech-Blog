<h1>hello</h1>
{{#each posts}}
<div class="card">
  <div class="card-body">
    <h1>{{title}}</h1>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <h1>{{body}}</h1>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <h1>{{User.username}}</h1>
  </div>
</div>

{{/each}}
{{!-- handlebars for(loop) above #each--}}
{{!-- look up boot strap and copy a card set up 
and put in classses try to make it look nice  --}}


